# Daily cadence (prices)

The daily command is the **price watch**, not this prompt.

`fpl-agent daily` and `fpl-agent prices` are deterministic. They do not call a language model.

The full news/squad review uses `prompts/predeadline.md` via `fpl-agent predeadline`, about one day before the official gameweek deadline.
